---
name: bookmark

services:
    ui:
        container_name: bookmark
        image: node:lts
        working_dir: /app
        volumes:
            - ./:/app
        tty: true
        environment:
            CONFIG_FILE: /app/config/config.yaml
        ports:
            - '5173:5173'
